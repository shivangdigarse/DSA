package array;

public class Aa_Basic {
    public static void main(String[] args) {
        //datatype[] variablename = new datatype[size];
        int[] rollno = new int[5];
        int[] rollno1 = { 1, 2, 3, 4, 5};
        //int[] rollno   declaration of array rollno is getting defined in stack
        //rollno = new int[5] initialisation: actually here object is being created in the heap(memory)
        //array objects in heap
        // objects are not continuous
        //so array may not be continuous, its depend on JVM
    }
}